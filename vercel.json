{
  "build": {
    "command": "npm run build",
    "outputDirectory": "dist"
  },
  "framework": "vite",
  "routes": [
    {
      "src": "/api/(.*)",
      "headers": {
        "Access-Control-Allow-Origin": "*"
      },
      "continue": true
    }
  ]
}
